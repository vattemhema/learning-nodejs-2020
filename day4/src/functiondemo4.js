'use strict';

// A function which accepts something and returns something //

const addValues = function (value1, value2){
    return value1 + value2;
}

let sum = addValues(245,654 );
console.log(`Sum ${sum}`);

sum = addValues(789,698);
console.log(`Sum ${sum}`);