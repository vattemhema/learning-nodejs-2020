'use strict';

// functions which accepts parameters and does not return a value //
// string introplotion === `Hello, ${name}`
const sayHello = function (name) {
    console.log(`Hello, ${name}`);
}

sayHello('vattem Hema');
sayHello('vignesh Iyer');
sayHello('Rama');
